{{> navbar}}
{{#if object}}
    <div class="optionsTableHeader _anim-items">
        <h1 id = "welcomeTextHeader">{{object.id}}</h1>
    </div>

   <div class="item _anim-items">
        <div class='row'>
            <div class="slideshow-container">
                <div class="mySlides{{object.id}} fade">
                    <img src="{{object.filePathFirst}}" >
                </div>

                <div class="mySlides{{object.id}} fade">
                    <img src="{{object.filePathSecond}}">
                </div>

                <div class="mySlides{{object.id}} fade">
                    <img src="{{object.filePathThird}}">
                </div>

                <div class="mySlides{{object.id}} fade">
                    <img src="{{object.filePathForth}}">
                </div>

                <div class="mySlides{{object.id}} fade">
                    <img src="{{object.filePathFifth}}">
                </div>

                <div class="mySlides{{object.id}} fade">
                    <img src="{{object.filePathSixth}}">
                </div>

                <div class="mySlides{{object.id}} fade">
                    <img src="{{object.filePathSeventh}}">
                </div>

                <!-- Next and previous buttons -->
                <a class="prev" onclick="plusSlides{{object.id}}(-1)">&#10094;</a>
                <a class="next" onclick="plusSlides{{object.id}}(1)">&#10095;</a>
                <script>
                    let slideIndex{{object.id}} = 1;
                    showSlides{{object.id}}(slideIndex{{object.id}});

                    // Next/previous controls
                    function plusSlides{{object.id}}(n) {
                    showSlides{{object.id}}(slideIndex{{object.id}} += n);
                    }

                    // Thumbnail image controls
                    function currentSlide{{object.id}}(n) {
                        showSlides{{object.id}}(slideIndex{{object.id}} = n);
                    }

                    function showSlides{{object.id}}(n) {
                        let i;
                        let slides{{object.id}} = document.getElementsByClassName("mySlides{{object.id}}");
                        if (n > slides{{object.id}}.length) {slideIndex{{object.id}} = 1} 
                        if (n < 1) {
                            slideIndex{{object.id}} = slides{{object.id}}.length
                        }
                        for (i = 0; i < slides{{object.id}}.length; i++) {
                            slides{{object.id}}[i].style.display = "none"; 
                        }
                        slides{{object.id}}[slideIndex{{object.id}}-1].style.display = "block"; 
                    }
                </script>
            </div>
        </div>
        <div class='row'>
            <div>
                <h2 id="Address{{object.id}}">Address</h2>
                <p> {{object.address}} </p>
                <hr>

                <h2 id="Price{{object.id}}">Price</h2>
                <p id="priceRent{{object.id}}"> {{object.price}} / per week</p>
                <hr>

                <h2 id="About{{object.id}}">About this property</h2>
                <p> {{object.description}} Lorem Ipsum is simply dummy text of the printing and typesetting industry. 
                    Lorem Ipsum has been the industry's standard dummy text ever since the 1500s, when
                    an unknown printer took a galley of type and scrambled it to make a type specimen book. 
                    It has survived not only five centuries, but also the leap into electronic typesetting, 
                    remaining essentially unchanged. It was popularised in the 1960s with the release of Letraset sheets containing Lorem 
                    Ipsum passages, and more recently with desktop publishing software like Aldus 
                    PageMaker including versions of Lorem Ipsum.</p>
            </div>
        </div>
        <button><a id="reqBtn{{object.id}}" class="button" href="#popupForm"> Request Exposé </a></button>
        </div>
    </div>
    <div id="popupForm" class="overlay">
        <div class="popup">
            <div class="input_form">
                <h2 id="formHeader{{object.id}}">Leave your contact</h2>
                <form class="go-bottom" action="/msg" method="post" id="form{{object.id}}">
                    <div>
                        <input id="object" name="object" type="text" value="{{object.id}}" required>
                        <label id="messageLabel{{object.id}}" for="object">Property ID</label>
                    </div>
                    <div>
                        <input id="name" name="name" type="text" required>
                        <label id="nameLabel{{object.id}}" for="name">Your full name</label>
                    </div>
                    <div>
                        <input id="email" name="email" type="text" required>
                        <label id="emailLabel{{object.id}}" for="email">Your E-mail address</label>
                    </div>
                    <div>
                        <input id="tel" name="tel" type="tel" >
                        <label id="telLabel{{object.id}}" for="tel">Your phone number</label>
                    </div>
                <button id= "reqSend{{object.id}}" type="submit" class="btn" onclick="personDataSaver()">Send</button>
                </form>
            </div>
            <a href="/">
              <div class="cl-btn-7"></div>
            </a>
        </div>
    </div>

{{else}}
<h2 id="emptyMsg">This page is empty now. 
    Visit it later...</h2>

{{/if}}
<script src="./js/animation.js"></script>